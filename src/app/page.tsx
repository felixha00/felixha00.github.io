import LogoScene from "@/components/3DLogo";

export const metadata = {
  title: "home | felix ha",
  description: "",
};

export default function Page() {
  return (
    <main>
      {/* <LogoScene /> */}
    </main>
  );
}